package samples.recursivedatastruct

object NodeList extends App {
  /*val n = new Node2(List(Node2(List(Node2(Nil, 3)), 2), Node2(Nil, 3)), 1)
  println(n)
  val n2 = n.nodes.flatMap(n => n.nodes).toList
  print(n2)*/
  // list of Int
}

/*
class Node2(val nodes: List[Node2], val value: Int)
object Node2 {
  def apply(nodes: List[Node2], value: Int): Node2 = new Node2(nodes, value)
}*/
